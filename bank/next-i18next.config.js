const path = require('path');

module.exports = {
  i18n: {
    locales: ['th-TH', 'en-US'],
    defaultLocale: 'en-US',
    // localeDetection: true,
  },
  react: {
    useSuspense: false,
    wait: true
  }
};
